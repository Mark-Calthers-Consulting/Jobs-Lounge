// Kept as a compatibility alias for older imports. The application table is the
// canonical implementation so status actions cannot drift into placeholder UI.
export { default } from './ApplicationsPageTable'
